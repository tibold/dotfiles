# ArchPillar professional -- slate and navy, calm cyan accent. Data-heavy, quiet.
set -g @bg          "#0F172A"   # --neutral-900
set -g @fg          "#94A3B8"   # --neutral-400
set -g @rule        "#1E293B"   # --neutral-800
set -g @rule_active "#0891B2"   # --accent

set -g @session_bg  "#0891B2"   # --accent
set -g @session_fg  "#FFFFFF"   # --accent-fg
set -g @window_bg   "#F1F5F9"   # --neutral-100
set -g @window_fg   "#0F172A"   # --neutral-900
set -g @metrics_bg  "#1E293B"   # --neutral-800
set -g @metrics_fg  "#94A3B8"   # --neutral-400
set -g @host_bg     "#334155"   # --neutral-700
set -g @host_fg     "#F1F5F9"   # --neutral-100
set -g @clock_bg    "#1E293B"   # --neutral-800
set -g @clock_fg    "#F1F5F9"   # --neutral-100
